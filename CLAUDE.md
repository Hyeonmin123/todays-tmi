# @today.tminformation 자동 릴스 프로젝트 - 작업 규칙

**영상·게시물·캡션·스케줄을 만들거나 고치기 전에 이 파일을 가장 먼저 읽고, 아래 규칙을 전부 지킨다.**
사용자가 새로 주의시키거나 강조한 내용은 즉시 이 파일에 추가한다.

## 0. 현재 상태
- 주제: **인물/위인의 의외의 사실**. 소재는 `content/bank_people.json` (사실 검증 + `sources` 필수).
- 발행: **릴스 + 사진 게시물(3장 캐러셀)을 같은 소재·같은 캡션·같은 스케줄**로 올린다. `config/settings.yaml` 의 `content_mode: reel_stock`.
- 영상과 게시물은 **미리 렌더·검수해서 커밋**해 둔다(`output/reels/<slug>/reel_stock.mp4`, `output/posts/<slug>/1~3.jpg`). CI 는 발행만 한다.
- 한쪽만 나간 소재(예: 릴스만)는 다음 실행에서 빠진 쪽만 발행한다(`state/log.json` 의 `media_id`=게시물, `reel_media_id`=릴스).
- 렌더: 릴스 `python -m src.reel_stock <slug> [track_index]`, 게시물 `python -m src.post_stock <slug>` (사진=Pixabay, 음성=ElevenLabs, 보이스 KKC HQ).

**소재 선정 기준(중요)**: 검증·사진 가능 여부 이전에 **'한국 사람 대부분이 이미 아는 상식인가'를 먼저 본다.** 교과서·드라마·밈으로 널리 알려진 이야기(안중근 단지동맹, 나폴레옹 키, 마리 앙투아네트 케이크 등)는 '의외의 사실'이 아니므로 만들지 않는다. 사용자가 '아는 사람 많다'며 반려한 전례가 있다(2026-09-08 이전 잡학, 2026-09-22 안중근).

## 1. 영상 규칙 (사용자가 강조한 것)
1. **길이 30초 이내**, 내용은 **진짜 핵심만**. (코드가 30초 초과 시 에러)
2. **존댓말**. 나레이션과 자막 모두.
3. **자막 줄 수**: 공백 뺀 글자 수 **12자 이하는 무조건 1줄**, 그보다 많으면 2줄 허용.
4. **숫자 발음**: 나레이션(`say`)에는 숫자를 **한글로 풀어 쓴다**(1889년 → "천팔백팔십구 년"). **자막은 숫자 그대로**(1889년).
5. **음성 검수는 내가 직접 한다**. 음성을 뽑으면 음성인식(ElevenLabs scribe)으로 다시 들어보고, 발음이 뭉개지면 **다시 뽑는다**(코드가 자동 재시도, 일치도 **0.97 이상**만 통과 - 0.93은 "그의 틀니는"→"그에 틀리는"처럼 뭉개진 것도 통과시켜서 올렸다). 음성인식이 "82살"처럼 적어도 고유어 수(여든두)와 비교되게 정규화한다. "검수는 못 한다/안 된다"고 말하지 않는다 - 이미 되는 방법이 있다.
6. **영상은 만든 뒤 내가 한 번 더 검수하고 문제 있으면 다시 만든 뒤에** 보고한다: 프레임 시트(`_qa.png`)로 자막 줄 수·잘림·사진이 소재와 맞는지, 길이, 음량, 음성인식 일치도.
7. **색**: 노란색 금지(어울리지 않는다고 지적받음). 신뢰감 있는 **블루** 계열(`ACCENT #2F6FE4`, 강조 글자 `#8EC5FF`).
8. **배지**: "오늘의 TMI" **글자만**(뒤 원형/알약 배경 없음). **CTA**: "친구에게 공유하기" **글자만**(뒤 원형 없음). 문구를 임의로 바꾸지 않는다.
9. **첫 프레임부터 훅**(빈 화면 금지). 컷마다 사진 + 큰 자막, 마지막에 반전 + CTA.
10. **사진**: Pixabay 사진 ID 를 소재에 **고정**(`img`, `hook_img`, `reveal_img`). 검색 순서가 바뀌어 엉뚱한 사진이 들어가는 일을 막는다. 소재와 안 맞는 사진(예: 폐허 복도)은 교체하고, 자막과 화면이 어긋나면(풍자 그림 vs 웅장한 초상화) 내용을 바꾼다.
10-2. **사진 선택 검수**: 후보 시트를 직접 보고 고른다. 스톡에 본인 사진이 없으면(루스벨트 등) 상징 이미지(국기·마이크)로 대체한다. **누드 조각상은 커버로 쓰지 않는다**(비율 크롭에 머리가 잘리고 노출 논란 소지) - 미켈란젤로는 망치 든 천사상·천장화로 대체했다. 4:5 커버에서 인물 머리가 잘리는 사진은 피한다.
10-3. **자막 폰트(도현체)에 없는 문자는 쓰지 않는다**: 『』「」는 □로 깨진다 → 작은따옴표('')로. (게시물 본문은 Pretendard라 괜찮지만 제목은 도현체.)
10-4. **음성 검수 통과가 안 되는 대사는 문장을 바꾼다**: 8번 다시 뽑아도 안 되면(예: '테오'→'터', '곳용'→'곤룡', 영어 'I have a dream', '466/64') 그 낱말·표기를 빼거나 풀어 쓴다. 개별 클립의 일치도 0.00은 무음을 '[배경음]'으로 읽은 오탐일 수 있으니 전체 영상 기준으로 다시 확인한다.
10-5. 사진은 **자동 선택 후 반드시 눈으로 검수**: 주식 앱·가면 쓴 사람·다른 인물 흉상('MILTIADES')·엉뚱한 졸업가운이 자동으로 뽑힌 적이 있다. 소재와 안 맞으면 교체.
11. 배경음은 `assets/audio` 로컬 mp3 5곡 순환(음량 낮게, 나레이션이 묻히면 안 됨). Pixabay 음악은 API 가 없다.

## 1-2. 게시물(사진 캐러셀) 규칙 (사용자가 정한 것, `content/bank_people.json` 의 `post_cards`)
- **C안**: 1장 커버 + 카드 **2장 = 총 3장**. 5장은 너무 많다고 지적받았다 → 늘리지 않는다.
- **커버**: 릴스 썸네일과 같은 Pixabay 사진 + **훅 문장**(훅에 인물 이름이 들어 있으니 이름 줄을 따로 넣지 않는다). 오른쪽 아래 → 화살표.
- **카드**: **사진을 어둡게 깔고**, 그 위에 **검은색 반투명 패널**(불투명도 약 60%, 뒤 사진이 살짝 비쳐야 함). 남색 패널은 쓰지 않는다.
- **설명은 간결하게 핵심만**(카드당 2~3줄). 자세한 내용은 캡션의 "영상에 다 담지 못한 이야기"에 있다.
- 마지막 카드에 **출처 + "저장해 두고 친구에게 공유하기"**. 블루 강조, 도현체 제목, 배지는 "오늘의 TMI" 글자만(릴스와 동일).
- 출처 줄이 길면 **줄바꿈**한다(패널 밖으로 잘리면 안 됨).
- 릴스와 **같은 소재, 같은 캡션, 같은 스케줄**. 게시물 렌더 후에도 프레임을 직접 검수(줄바꿈·잘림·사진 매칭)한 뒤 보고한다.

## 2. 캡션 규칙
- **영상에 담지 못한 자세한 내용을 서술**한다(날짜·인물·경위·다른 설 등). 형식: 질문 한 줄 → `📌 영상에 다 담지 못한 이야기` → 상세 → 출처 → 해시태그 5개.
- **출처로 확인된 사실만** 쓴다. 확신이 낮으면 "전해져요/설이 있어요/추정" 으로 쓰고 단정하지 않는다. 확인 못 한 건 뺀다.
- 사실 검증은 **내가 알아서 웹 검색으로** 한다. 기억에 의존해 쓴 부분은 발행 전에 반드시 검색으로 재확인한다(세종 사례: 신하가 권한 게 아니라 태종의 유언이었음).
- 캡션 첫 60자는 이전 게시물과 겹치지 않게(발행 안전장치가 앞 60자로 중복을 판별한다).

## 3. 발행·스케줄 규칙
- 각 슬롯은 **목표 시각 2시간 전부터 1시간 간격 3번, 마지막 크론 = 목표 시각**. 현재: 06/07/**08**, 12/13/**14**, 17/18/**19** (KST). 슬롯 시작은 6, 12, 17.
- **슬롯 창(`slot_tolerance_hours` 4.5h)끼리 겹치면 안 된다** → 슬롯 간격 ≥ 6시간. (겹치면 아침 발행이 점심 슬롯으로 잘못 잡힌다.)
- GitHub 크론은 1~7시간 늦게 돈다. 스케줄을 **복잡하게 만들지 않는다** - 사용자가 "그냥 2시간씩 당겨"처럼 단순한 방식을 원했다. 사용자가 말한 그대로 적용하고 해석을 넓히지 않는다.
- `media_publish` 가 에러를 줘도 실제로는 발행되는 일이 잦다 → `find_recent_media` 안전장치 유지. 재시도 전에 **인스타 API 로 실제 발행 상태를 먼저 확인**한다.
- 사고(중복 발행 의심 등)가 나면 **자동 발행부터 즉시 끄고**(`gh workflow disable publish.yml`) 진단한다. 사용자의 과장된 표현에 휘둘리지 말고 실제 API 로 사실 확인 후 정확히 보고한다.
- 강제 발행: `gh workflow run publish.yml -f force=true -f dry_run=false`. 발행 후 인스타 API 로 릴스 존재·캡션을 확인해 보고한다.
- 다시 올릴 소재는 `state/log.json` 의 `published` 에서 해당 slug 를 빼야 한다.

## 4. 작업 방식
- 키·토큰은 사용자가 채팅에 붙여넣으면 `config/secrets.env`(gitignore) 와 GitHub Secrets 에 **내가 적용**한다. 로그·답변에 값을 출력하지 않는다. `sk_` 로 시작하지 않는 ElevenLabs 값은 키 ID 이므로 진짜 키를 요청한다.
- 사용자가 "영상을 만들지 말고 대본 먼저"라고 하면 영상 렌더 없이 대본만 낸다.
- 답변은 **요점(결론)을 먼저**, 짧게. 사용자가 "요지가 뭐냐"고 물으면 결론부터 다시 말한다.
- 커밋·푸시는 발행에 필요한 파일(영상, 뱅크, 설정)을 반영할 때만 하고, 그 외에는 사용자가 요청할 때 한다.
- 자동 발행을 켜고 끄는 것, 삭제 같은 되돌리기 어려운 조치는 사용자 지시 범위 안에서만 한다.
